# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.genkit/*
.env*
!.env.example

# firebase
firebase-debug.log
firestore-debug.log

# dev artifacts
.dev-server*.log
.modified
# Firebase/Next.js build artifacts (too large for GitHub)
.firebase/
.next/
out/
build/

# qwen
.qwen/

# ── GSD baseline (auto-generated) ──
.gsd-id
.mcp.json
.bg-shell/
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace
.env
.env.*
node_modules/
dist/
__pycache__/
*.pyc
.venv/
venv/
target/
vendor/
*.log
coverage/
.cache/
tmp/

# ── GSD baseline (auto-generated) ──
nul
nul.*
# Shell artifact files
^
con
con.*
prn
prn.*
aux
aux.*
com[1-9]
com[1-9].*
lpt[1-9]
lpt[1-9].*

# Global AI skills live in ~/.skills/ — per-project copies are non-canonical
# and should never be tracked. Files remain on disk for agent use.
/.claude/skills/
/.agents/skills/
# Harness manifests that skill installers write into a project (impeccable's
# hooks install .claude/settings.local.json, .codex/hooks.json, .cursor/ and
# .grok/ variants). These are machine-local by design — impeccable's own docs
# assume .claude/settings.local.json is gitignored. 97 such files were tracked
# here; untracked 2026-09-21 after Claude and Codex were retired on both
# machines (Cursor and Grok are unused). Files stay on disk for agent use.
/.claude/
/.codex/
/.cursor/
/.grok/
# Impeccable skill artifacts
.impeccable/
.audit-passes/
.pi/
.pi-subagents/
_capture/

# Local-only secret handoff (never commit)
.cron-secret.txt
