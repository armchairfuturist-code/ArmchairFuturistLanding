# The Armchair Futurist - Landing Page

@AGENTS.md

Single source of truth is `AGENTS.md` — read it first. Do not deploy from `firebase deploy` CLI assumptions: deploys run on push to `main` via `.github/workflows/deploy.yml`.
